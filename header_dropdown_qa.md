# 導覽分類下拉選單 QA 紀錄

## 本機首次桌面檢查

頁面：`http://localhost:3102/?headerdropdown=20260826`。預設桌面視窗中的 Header 已顯示新的「產品分類」觸發按鈕、下拉箭嘴及既有首頁／結帳／搜尋／購物籃／語言控制。自動測試、產品驗證與正式建置均已通過。第一次自動化點擊觸發按鈕後，擷取畫面未見 Mega Menu 保持展開；其後以更精確的 DOM 互動重測，確認問題來自 hover 先展開後的點擊反向關閉，而非選單內容或路由問題。

修正後，桌面分類觸發按鈕的 `aria-expanded` 為 `true`，下拉面板正常呈現貓咪專區、狗狗專區與精選導覽三個區段；其內連結對應既有罐罐、乾糧、小食、投藥小食及所有產品頁。為避免 desktop hover 先展開而隨後點擊反向收起，桌面按鈕現在採取「點擊只開啟、點擊外部／按 Escape／游標離開才關閉」的操作規則。

後續 375×812 真實手機寬度互動測試確認，點擊手機抽屜中的「產品分類」後，貓咪專區、狗狗專區與精選導覽會在抽屜內完整顯示，抽屜本身可垂直捲動，既有關閉按鈕與背景遮罩仍維持可用。測試期間發現以固定高度過渡呈現的手機面板會在該抽屜結構中被計算為零高度，因此已改成以開啟狀態條件式掛載內容並套用短暫淡入／位移動畫；這不會改變主 Header 高度，也避免觸控裝置出現空白面板。

桌面與手機分類內容均只使用已存在的貓咪、狗狗、全部產品及品種導覽路由。最終本機桌面互動核對顯示，主導覽的「產品分類」按鈕可展開不造成 Header 高度改變的浮層 Mega Menu。選單顯示的子分類連結包括貓咪罐罐／濕糧、貓乾糧／主糧、貓貓小食、狗狗食品、狗狗小食與投藥餵藥專用小食；連結 URL 均對應既有 `/categories/[slug]/[sub]` 路由。按鈕、浮層與所有分類連結於頁面可存取樹中正常呈現。
