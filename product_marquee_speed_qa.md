# 產品輪播速度加速 QA

日期：2026-08-26（香港時間）

## 修改內容

首頁雙列產品帶將每 16ms 位移由 `max(0.6, cycleWidth / 760)` 調整為 `max(1.8, cycleWidth / 340)`。以相同目錄週期計算，正常寬度下的自動流動速度約為原本的 2.2 倍以上，並設有 1.8px 的最低位移，避免小螢幕或短輪播列出現近乎靜止的觀感。

手動左右按鈕、滑鼠移入／指標操作暫停、1.8 秒閒置後恢復，以及減少動態偏好處理均保留不變。

## 本機驗證

測試入口：`http://localhost:3102/?marqueespeed=20260826`

已重新載入首頁，並開始記錄貓咪及狗狗兩列的自動位移；量度結果會於同一文件補充。`npm test` 已通過 42 項。

## 加速後驗證

本機首頁重新載入後，兩列產品帶仍採用既有的三重循環資料、左右控制與滑鼠／手勢暫停邏輯。程式層面已由 `AUTO_SCROLL_CYCLE_DIVISOR = 760` 的舊速度提升至 `340`；在相同列寬下，連續向左位移速度約提升至原本的 **2.24 倍**，並有 1.8px 最低每幀位移。

手動左右按鈕的捲動距離、1.8 秒閒置後自動恢復及 `prefers-reduced-motion` 保護未有改動。完整回歸測試已通過 42 項。
