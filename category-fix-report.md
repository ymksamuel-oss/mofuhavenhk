# Mofu Haven 貓咪商品分類與搜尋修復報告

**作者**：Manus AI  
**日期**：2026 年 8 月 19 日  
**專案版本**：`4eb217a9`  

---

## 一、 問題診斷與後台資料庫狀態

經由後台資料庫與 Stripe Live API 的完整盤點，我們確認了目前系統的資料架構：

1. **後台資料庫（MySQL / TiDB）**：專案資料庫目前僅存放會員認證與用戶授權相關表格（例如 `users`），並**沒有建立專屬的商品資料表**。
2. **商品資料實際來源**：網站的所有商品與價格，皆直接透過後端 API 從已核實的 **Stripe Live 帳戶**（共 91 件 Active 商品及 144 個有效價格）動態載入，並在本地維持一份經過 MCP 核實的唯讀快照作為安全降級資料源。
3. **「呢個分類暫時未有商品」的根本原因**：
   - 舊版的首頁分類導覽卡片與頁首導覽均使用 `#` 作為佔位符，並未將使用者導向帶有分類查詢參數的路由。
   - Stripe 產品的 `metadata` 欄位同時包含舊的英文 slug、中文標籤與部分被 legacy 資料複寫的 `slug=wet-cans` 屬性，導致前端在篩選「貓咪商品」或「罐罐」時，無法正確對應分類而返回空陣列。

---

## 二、 解決方案與修復內容

我們已完成以下全端程式碼修復，並通過了 TypeScript 檢查、Vitest 自動化測試以及桌面／手機版介面驗證：

1. **統一分類與搜尋正規化（`shared/productCatalog.ts`）**：
   - 建立強健的分類正規化邏輯，兼容英文 slug（`cats`、`dogs`、`treats`、`wet-cans` 等）與中文標籤。
   - 針對 `wet-cans`（貓咪罐罐）實施嚴格的名稱證據過濾，排除被舊系統 metadata 誤標的乾糧、脫水零食與脆條，確保分類內容精準。
2. **前後端路由與篩選打通**：
   - 將首頁的 9 個分類卡片及頁首「產品」導航改為真正的查詢路由（例如 `/?category=cats#products`）。
   - 在商品區塊新增即時搜尋輸入框、分類標籤列、清除篩選按鈕與友善的空結果提示。
3. **自動化測試與驗證結果**：
   - 通過 7 項 Vitest 單元測試，涵蓋預設價格選取、欄位映射、分類正規化與關鍵字搜尋回歸。
   - 本地 API 實際測試結果：
     - **貓咪商品（`cats`）**：共 **77 件**
     - **寵物零食（`treats`）**：共 **61 件**
     - **貓咪罐罐（`wet-cans`）**：共 **9 件**（已精準排除非罐罐商品）
     - **關鍵字搜尋（`CIAO`）**：共 **35 件**

---

## 三、 正式域名（`mofuhavenhk.com`）部署注意事項

目前 Manus 預覽版本（`mofuhaven-5gysmfvo.manus.space`）與各分類頁面均已完整呈現 91 件商品與正確的分類篩選功能。

然而，經系統偵測，正式域名 **`mofuhavenhk.com`** 目前仍然指向舊有的 Vercel／Next.js 部署，尚未切換至本次 Manus 全端專案。如果您在正式網域訪問時仍看到舊畫面或無法瀏覽分類，請前往本專案管理介面的 **Settings → Domains**，重新綁定 `mofuhavenhk.com`，並根據該頁面提供的最新 DNS 記錄更新 GoDaddy 設定，即可讓正式網域同步顯示最新修復版本。
