# UI/UX 修正驗證紀錄

## 商品頁

桌面及 390px 手機 Preview 均顯示商品卡片已移除商品簡介段落，保留品牌名稱、兩行商品標題、價格及「加入購物車」按鈕。手機卡片採較細圖片內距、較細內容 padding 及較短按鈕高度，首屏可見的商品數量增加。

在 `/products?category=cat&q=雞肉` 中，搜尋框仍正確顯示「雞肉」，並載入符合搜尋結果的貓咪商品；主分類與子分類列保持可見。

## 探索寵物世界

桌面及 390px 手機 Preview 均成功渲染頁籤「貓咪專區」、「狗狗專區（即將推出）」及「小寵物專區（即將推出）」。貓咪專區預設顯示 12 種品種內容，手機畫面可見第一張貓咪品種圖片已載入；頁面仍保留日常飼養指南入口。

## 建置

Vitest 7 個 test files、32 tests 通過；production build 成功完成。建置僅出現既有 hero-pattern runtime asset 提示及 chunk size warning，沒有編譯錯誤。

## Playwright 互動驗證

使用手機 viewport 實測 `/products?category=cat&q=雞肉` 後點擊精確「狗狗商品」按鈕，結果 URL 變為 `/products?category=dog`、搜尋框清空且沒有 `q` 參數。商品卡片包含品牌、商品標題、價格及「加入購物車」，沒有已移除的「查看商品詳情」文字。

PetWorld 實測確認三個頁籤存在，貓咪品種圖片元素數量為 12；點擊狗狗專區顯示「狗狗專區即將推出」，再切回貓咪專區後「12 種常見貓咪的相處提示」正常顯示。
